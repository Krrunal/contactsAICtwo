import {
  Dimensions,
  FlatList,
  Image,
  Modal,
  SafeAreaView,
  StyleSheet,
  Text,
  TextInput,
  TouchableOpacity,
  TouchableWithoutFeedback,
  View
} from 'react-native';
import styled, { ThemeProvider } from "styled-components/native";

import { COLORS } from '../../../src/containers/theme/Colors';
import Font from '../../../src/containers/theme/font';
import Metrics from '../../../src/containers/theme/Metrics';
import PropTypes from 'prop-types';
import React from 'react';
import checked from "../../../src/assets/icons/checked.png";
import { color } from 'react-native-reanimated';
import { connect } from "react-redux";
import cross from "../../../src/assets/icons/close.png";
import data from './Countries';

var {width,height} =  Dimensions.get('window');

 class IntlPhoneInput extends React.Component {
  constructor(props) {
    super(props);
    const defaultCountry = data.filter((obj) => obj.code === props.defaultCountry)[0] || data.filter((obj) => obj.code === 'TR')[0];
    this.state = {
      defaultCountry,
      flag: defaultCountry.flag,
      modalVisible: false,
      dialCode: defaultCountry.dialCode,
      phoneNumber: '',
      mask: defaultCountry.mask,
      countryData: data,
      selectedCountry:defaultCountry
    };
  }

  onChangePropText=(unmaskedPhoneNumber, phoneNumber) => {
    const { dialCode, mask,selectedCountry } = this.state;
    const countOfNumber = mask.match(/9/g).length;
    if (this.props.onChangeText) {
      const isVerified = countOfNumber === unmaskedPhoneNumber?.length && phoneNumber?.length > 0;
      this.props.onChangeText({
        dialCode, unmaskedPhoneNumber, phoneNumber, isVerified,selectedCountry
      });
    }
  }

  onChangeText = (value) => {
    let unmaskedPhoneNumber = (value.match(/\d+/g) || []).join('');

    if (unmaskedPhoneNumber.length === 0) {
      this.setState({ phoneNumber: '' });
      this.onChangePropText('', '');
      return;
    }


    let phoneNumber = this.state.mask.replace(/9/g, '_');
    for (let index = 0; index < unmaskedPhoneNumber.length; index += 1) {
      phoneNumber = phoneNumber.replace('_', unmaskedPhoneNumber[index]);
    }
    let numberPointer = 0;
    for (let index = phoneNumber.length; index > 0; index -= 1) {
      if (phoneNumber[index] !== ' ' && !isNaN(phoneNumber[index])) {
        numberPointer = index;
        break;
      }
    }
    phoneNumber = phoneNumber.slice(0, numberPointer + 1);
    unmaskedPhoneNumber = (phoneNumber.match(/\d+/g) || []).join('');

    this.onChangePropText(unmaskedPhoneNumber, phoneNumber);
    this.setState({ phoneNumber });
  }


  showModal = () => (this.props.disableCountryChange ? null : this.setState({ modalVisible: true }));

  hideModal = () => this.setState({ modalVisible: false });

  onCountryChange = async (code) => {
    const countryData = await data;
    try {
      const country = await countryData.filter((obj) => obj.code === code)[0];
      this.setState({
        dialCode: country.dialCode,
        flag: country.flag,
        mask: country.mask,
        phoneNumber: '',
        selectedCountry:country
      });
      this.hideModal();
    } catch (err) {
      const defaultCountry = this.state.defaultCountry;
      this.setState({
        dialCode: defaultCountry.dialCode,
        flag: defaultCountry.flag,
        mask: defaultCountry.mask,
        phoneNumber: '',
        selectedCountry:defaultCountry
      });
    }
  }

  filterCountries = (value) => {
   const { lang
  } = this.props;
    const countryData = data.filter((obj) => (obj[lang?.toLowerCase()??"en"]?.indexOf(value) > -1 || obj.dialCode.indexOf(value) > -1));
    this.setState({ countryData });
  }

  focus() {
    this.props.inputRef.current.focus();
  }

  renderModal=() => {
    if (this.props.customModal) return this.props.customModal(this.state.modalVisible,this.state.countryData,this.onCountryChange);
    const {
      countryModalStyle,
      modalContainer,
      modalFlagStyle,
      filterInputStyle,
      modalCountryItemCountryNameStyle,
      modalCountryItemCountryDialCodeStyle,
      closeText,
      filterText,
      searchIconStyle,
      closeButtonStyle,
      lang
    } = this.props;
    
    return (
      <Modal animationType="slide" transparent={false} visible={this.state.modalVisible}>
        <SafeAreaView style={{ flex: 1 }}>
        <View style={[styles.modalContainer, modalContainer]}>
          <View style={styles.filterInputStyleContainer}>
            <TextInput autoFocus onChangeText={this.filterCountries} placeholder={filterText || 'Filter'} style={[styles.filterInputStyle, filterInputStyle]} />
            <Text style={[styles.searchIconStyle, searchIconStyle]}>🔍</Text>
          </View>
          <FlatList
            style={{ flex: 1 }}
            data={this.state.countryData}
            keyExtractor={(item, index) => index.toString()}
            renderItem={
          ({ item }) => (
            <TouchableWithoutFeedback onPress={() => this.onCountryChange(item.code)}>
              <View style={[styles.countryModalStyle, countryModalStyle]}>
                <Text style={[styles.modalFlagStyle, modalFlagStyle]}>{item.flag}</Text>
                <View style={styles.modalCountryItemContainer}>
                  <Text style={[styles.modalCountryItemCountryNameStyle, modalCountryItemCountryNameStyle]}>{item[lang?.toLowerCase()??"en"]}</Text>
                  <Text style={[styles.modalCountryItemCountryDialCodeStyle, modalCountryItemCountryDialCodeStyle]}>{`  ${item.dialCode}`}</Text>
                </View>
              </View>
            </TouchableWithoutFeedback>
          )
        }
          />
        </View>
        <TouchableOpacity onPress={() => this.hideModal()} style={[styles.closeButtonStyle, closeButtonStyle]}>
          <Text style={styles.closeTextStyle}>{closeText || 'CLOSE'}</Text>
        </TouchableOpacity>
        </SafeAreaView>
      </Modal>
    );
  }

renderAction=()=>{
  const renderAction=this.props.renderAction;
  if(renderAction) {
    console.log("action",renderAction);
    if(typeof renderAction!=="function") throw ("The renderAction is not a function. Please set a renderAction function on there");
    else return this.props.renderAction();
  }
  return null;
}

  render() {
    const { flag } = this.state;
    const {
      containerStyle,
      flagStyle,
      phoneInputStyle,
      dialCodeTextStyle,
      inputProps,
      icon,
      containerLogin
    } = this.props;
    return (
      <View>
          <ThemeProvider theme={this.props.theme}>

            <View style={styles.userText}>
              {/* <BoldBlack>*Phone number :</BoldBlack> */}
            
              <View style={styles.RigthView}>
                <TouchableOpacity onPress={() => this.showModal()} style={{  width:width * 0.25,}}>
              <View style={{flexDirection:'row'}}>
                {this.props.isShowLabel == false  ?
                (
                 this.props.theme.mode === "light" ? (
                    <Image source={require('../../../src/assets/icons/iSquare.png')} style={styles.infoIcon} />
                    ) : (
                      <Image source={require('../../../src/assets/icons/iSquareWhite.png')} style={styles.infoIcon} />
                    )
                ):null }
             
                  {this.props.isShowLabel == false  ?
                  (  this.props.theme.mode === "light" ? (
                    <Text style={[styles.reqtext,{color:COLORS.black, marginLeft: width * 0.01,}]}>
                      Select Country
                    </Text>
                  ) : (
                    <Text style={[styles.reqtext,{color:COLORS.white, marginLeft: width * 0.01,}]}>
                    Select Country 
                    </Text>
                  )): null  }
                     </View>
                     {this.props.isShowLabel == false ?
                  (  this.props.theme.mode === "light" ? (
                    <Text style={[styles.reqtext,{color:COLORS.black, marginLeft: width * 0.05,}]}>
                     Code
                    </Text>
                  ) : (
                    <Text style={[styles.reqtext,{color:COLORS.white, marginLeft: width * 0.05,}]}>
                  Code
                    </Text>
                  )): null  }
                </TouchableOpacity>
              </View>
            </View>

            
   {this.props.isLogin == false || this.props.isShowLabel == false ?
       <View style={{ ...styles.containerLogin, ...containerStyle }}>
            <View>
                <Text style={{ fontSize: width * 0.028,fontFamily: Font.regular,color: COLORS.main_text_color, marginLeft: Metrics.smallMargin, marginTop: 1}}>Phone Number</Text>
            </View>
           <View style={{flexDirection:'row'}}>
         
                      {this.props.isShowLabelManually == false || this.props.isLogin == false? 
                              ( <TouchableOpacity style={styles.openDialogView}  onPress={() => this.showModal()}>
                                    <Text style={[styles.dialCodeTextStyle, dialCodeTextStyle]}>{this.props.dialCode ? this.props.dialCode : this.state.dialCode }</Text>
                                </TouchableOpacity>
                      ) :  (
                              <TouchableOpacity style={styles.openDialogView} >
                                    <Text style={[styles.dialCodeTextStyle, dialCodeTextStyle]}>{this.props.dialCode ? this.props.dialCode : this.state.dialCode }</Text>
                              </TouchableOpacity> )}
                        {this.renderModal()}
                            <TextInput
                              {...inputProps}
                              style={[styles.phoneInputStyle, phoneInputStyle]}
                              // placeholder={this.props.placeholder || this.state.mask.replace(/9/g, '_')}
                              placeholder={ this.props.isProfile == false  ? " (303) 123-4567" : "Phone Number"}
                              placeholderTextColor={COLORS.main_text_color}
                              autoCorrect={false}
                              keyboardType="number-pad"
                              secureTextEntry={false}
                              value={this.props.value}
                              // value={this.state.phoneNumber}
                              onChangeText={this.onChangeText}
                            />
                              </View>
                            {this.renderAction()}
                            <Image source={this.props.icon} style={styles.checkIcon}/>
                </View> :  
                    <View style={{ ...styles.container, ...containerStyle }}>
                    {this.props.isShowLabelManually == false ?  
                    
                     ( <TouchableOpacity style={styles.openDialogView}  onPress={() => this.showModal()}>
                           <Text style={[styles.dialCodeTextStyle, dialCodeTextStyle]}>{this.props.dialCode ? this.props.dialCode : this.state.dialCode }</Text>
                       </TouchableOpacity>
                    ):  (
                   <TouchableOpacity style={styles.openDialogView}   >
                         <Text style={[styles.dialCodeTextStyle, dialCodeTextStyle]}>{this.props.dialCode ? this.props.dialCode : this.state.dialCode }</Text>
                   </TouchableOpacity> )}
                    {this.renderModal()}
                         <TextInput
                           {...inputProps}
                           style={[styles.phoneInputStyle, phoneInputStyle]}
                           // placeholder={this.props.placeholder || this.state.mask.replace(/9/g, '_')}
                           placeholder={ this.props.isProfile == false  ? " (303) 123-4567" : "Phone Number"}
                           placeholderTextColor={COLORS.main_text_color}
                           autoCorrect={false}
                           keyboardType="number-pad"
                           secureTextEntry={false}
                           value={this.props.value}
                           // value={this.state.phoneNumber}
                           onChangeText={this.onChangeText}
                         />
                         
                         {this.renderAction()}
                         <Image source={this.props.icon} style={styles.checkIcon}/>
                         </View>
                
                }


  
 
         
          </ThemeProvider>

      </View>

    );
  }
}

IntlPhoneInput.propTypes = {
  lang: PropTypes.string,
  defaultCountry: PropTypes.string,
  onChangeText: PropTypes.func,
  customModal: PropTypes.func,
  phoneInputStyle: PropTypes.object, // {}
  containerStyle: PropTypes.object, // {}
  dialCodeTextStyle: PropTypes.object, // {}
  flagStyle: PropTypes.object, // {}
  icon: PropTypes.object,
  value: PropTypes.string,

  modalContainer: PropTypes.object, // {}
  filterInputStyle: PropTypes.object, // {}
  closeButtonStyle: PropTypes.object, // {}
  modalCountryItemCountryNameStyle: PropTypes.object, // {}
  filterText: PropTypes.string,
  closeText: PropTypes.string,
  searchIconStyle: PropTypes.object,
  disableCountryChange: PropTypes.bool,
  inputRef: PropTypes.object,
};


const styles = StyleSheet.create({
  closeTextStyle: {
    padding: 5,
    fontSize: 20,
    color: 'black',
    fontWeight: 'bold'
  },
  modalCountryItemCountryDialCodeStyle: {
    fontSize: 15
  },
  modalCountryItemCountryNameStyle: {
    flex: 1,
    fontSize: 15
  },
  modalCountryItemContainer: {
    flex: 1,
    paddingLeft: 5,
    flexDirection: 'row'
  },
  modalFlagStyle: {
    fontSize: 25,
  },
  modalContainer: {
    paddingTop: 15,
    paddingLeft: 25,
    paddingRight: 25,
    flex: 10,
    backgroundColor: 'white'
  },
  flagStyle: {
    fontSize: 35,
  },
  dialCodeTextStyle: {
    fontSize: width * 0.032,
    fontFamily: Font.regular,
    color: COLORS.main_text_color, 
   // marginLeft: Metrics.smallMargin

  },
  countryModalStyle: {
    flex: 1,
    borderColor: 'black',
    borderTopWidth: 1,
    padding: 12,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between'
  },
  openDialogView: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  filterInputStyle: {
    flex: 1,
    paddingTop: 10,
    paddingBottom: 10,
    backgroundColor: '#fff',
    color: '#424242',
  },
  searchIcon: {
    padding: 10,
  },
  filterInputStyleContainer: {
    width: '100%',
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
  },
  phoneInputStyle: {
    marginLeft: Metrics.baseMargin,
    flex: 0.93,
    padding: 0,
    // borderWidth: 1
    // width: width * 0.6
  },
    
  containerLogin: {
  
    paddingLeft: 12,
    // padding: 5,
    borderRadius: 10,

    backgroundColor: 'white',
    //marginTop:7,
    width: width * 0.9,


  }, 
  container: {
  
    paddingLeft: 12,
    // padding: 5,
    borderRadius: 10,
 alignItems: 'center',
    backgroundColor: 'white',
    //marginTop:7,
    width: width * 0.9,
  flexDirection: "row",
   // marginTop: Metrics.smallMargin,

  },
  searchIconStyle: {
    color: 'black',
    fontSize: 15,
    marginLeft: 15
  },
  buttonStyle: {
    alignItems: 'center',
    padding: 14,
    marginBottom: 10,
    borderRadius: 3,
  },
  buttonText: {
    fontSize: 18,
    fontWeight: 'bold',
    color: 'black',
  },
  countryStyle: {
    flex: 1,
    borderColor: 'black',
    borderTopWidth: 1,
    padding: 12,
  },
  closeButtonStyle: {
    padding: 12,
    alignItems: 'center',
  },
  infoIcon: {
    width: width * 0.04,
    height: width * 0.04,
    alignSelf: 'center',
    resizeMode:'contain'

  },
  userText:{
    marginTop:Metrics.smallMargin,
    marginLeft:Metrics.baseMargin,
    flexDirection:'row'
  },
  normalText:{
    fontFamily: Font.medium,
    marginTop:Metrics.xsmallMargin,
    fontSize: width*0.04,
  },
  RigthView:{
    
    flex:1,
    flexDirection: 'row',
    justifyContent: 'flex-end',
    // marginTop:Metrics.smallMargin,
  alignItems:'center',
   marginRight:Metrics.baseMargin,
    position:"relative",
    top:0,
 

  },
  checkIcon: {
    width: width * 0.045,
    height: width * 0.045,
    resizeMode: 'contain'
  },
  reqtext:{
    fontSize:width*0.030,
  
   fontFamily: Font.regular,
}

});

function mapStateToProps (state) {
  // console.log("Intel======>",state.reg.loading);
  // console.log("State From Sign -- in------->",state.reg)
  // console.log("State From Log -- in------->",state.login)


  return{
  theme: state.themeReducer.theme,
  contactMsg: state.reg.contactMsg.status,
  shouldLoadData: state.login.shouldLoadData,
  loading: state.login.loading,
  loadingSignup:state.reg.loadingSignup,
}};

export default connect(mapStateToProps)(IntlPhoneInput);
const CountryText = styled.Text`
  margin-left: 5px;
  font-family: Roboto-light;
  font-size: 8px;
  color: ${(props) => props.theme.iconColor};
`;
const BoldBlack = styled.Text`
  font-family: Roboto-Medium;
  font-size: 17px;
  color: ${(props) => props.theme.iconColor};
`;
